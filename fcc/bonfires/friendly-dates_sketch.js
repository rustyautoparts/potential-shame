if (startYear == endYear) {
  //remove startYear
  if (currentYear == startYear) {
    //remove endYear
  }
  if (startMonth == endMonth) {
    //remove endMonth
    if (startDay == endDay) {
      //remove endDay
    }
  }
} else if (currentYear == startYear &&
    startMonth == 'December' &&
    startYear == endYear + 1) {
  // remove start year
  // remove end year
}
